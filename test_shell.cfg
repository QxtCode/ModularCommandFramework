# test_shell v2.5 Configuration
# 配置文件放在可执行文件同一目录，启动时自动加载。
# 命令行覆盖：--pool_size=16 --workers=8

# 引擎
pool_size = 8
workers = 4

# 插件目录
plugin_dir = plugins

# 日志级别: debug | info | error | fatal
log_level = info

# 队列积压告警（0 = 不告警）
queue_warn = 0
