# 性能与极限 — test_shell v2.5

> 测试环境: Intel i7-13700H (16 逻辑核心), Debug 编译, 跨平台

## 峰值吞吐

| 指标 | 值 | 说明 |
|------|-----|------|
| **峰值吞吐** | ~23,000 cmd/s | 全速注入, 16槽池, 8工人 |
| **瓶颈** | 主循环周期 (100ms cv 超时) | 每条命令需 2 周期: 输入→处理 + 完成→drain |
| **理论最大** | POOL_SIZE × 10 周期/秒 | 16 槽: ~160 cmd/s 客户可见, 内部更高 |

### 测量方式

```bash
./test_runner --gtest_filter=PeakStressTest.RampUpThroughput
```

1 秒全速注入, 统计 ResultStore 中实际完成的结果数。

## 持续负载

| 指标 | 值 |
|------|-----|
| **时长** | 10 秒 @ ~200 cmd/s 注入速率 |
| **内存增量** | < 50 MB (GetProcessMemoryInfo) |
| **池恢复** | 完全 — 所有 Task 测试后归还池子 |

## 延迟特征

| 百分位 | 约值 | 原因 |
|--------|------|------|
| P50 | ~100ms | 一个主循环周期 |
| P95 | ~300ms | 两个周期 + 线程调度 |
| P99 | ~500ms | 最大等待 + OS 调度抖动 |

延迟主要来源是 `ShellEngine::WaitForWork()` 的 100ms 超时。
命令在工人线程上快速完成，但结果可见性取决于主循环下一轮 `DrainResults()`。

## 并发与安全

| 测试 | 结果 |
|------|------|
| **Slot 异常捕获** | ✅ 进程存活 — `Slot::Run` 中的 `catch(...)` 标记 slot 为 dead |
| **爆发过载** | ✅ 500 条 burst, 池完全恢复 |
| **过载恢复** | ✅ 过载 → 空闲, 3 秒内恢复 |
| **并发 Push** | ✅ 2 推送者 + 2 拉取者, 0 错误, 231K 次操作 |
| **TOCTOU 竞态** | ✅ 4 线程 AddModule/UnloadModule, 零僵尸信号 |

## 限制与调优

### 池耗尽

当所有 Task 槽位被占用时, `TasksPool::Acquire()` 返回 `nullptr`。
引擎输出 `[ERROR] All tasks busy` 并跳过命令。

**调优:** 增大 `pool_size` 或 `--pool_size=N`。

### 工人饱和

当所有工人忙碌时, 任务在线程池中排队。线程池队列无限 —
极端情况下可能导致内存增长。

**调优:** 增大 `workers`。经验公式: `workers = CPU核心数 - 2`。

### 主循环周期

`WaitForWork()` 超时 (100ms) 是结果可见性的最小延迟。
降低超时可减少延迟, 但会增加空闲 CPU 使用。

**调优:** 修改 `ShellEngine::WaitForWork()` 超时参数。

## 配置参考

配置由 `ShellConfig` 在 `main.cpp` 启动时加载。  
加载流程见 [07-架构概览](07-架构概览.md)。

### test_shell.cfg（放在可执行文件旁边）

```ini
# 引擎
pool_size = 8
workers = 4

# 路径
plugin_dir = plugins

# 日志级别 (debug | info | error | fatal)
log_level = info

# 告警 (0 = 不告警)
queue_warn = 0
```

### 命令行覆盖

```bash
./test_shell --pool_size=32 --workers=12 --plugin_dir=./my_plugins
```

优先级: 命令行 > 配置文件 > 默认值。

## 测试基线

所有性能断言在 `test/core/peak_stress_test.cpp` 中验证:

```bash
# 仅运行性能测试
./test_runner --gtest_filter=PeakStressTest.*

# 预期: 6 tests, 全部 PASSED
```
